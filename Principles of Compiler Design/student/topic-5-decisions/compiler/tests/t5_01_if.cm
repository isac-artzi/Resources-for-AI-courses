/* Expected output: 1  20  99 */
int main() {
    int x;
    x = 5;
    if (x > 0) { print(1); }
    if (x > 100) { print(10); } else { print(20); }
    if (x > 100) { print(0); } else { if (x > 3) { print(99); } else { print(1); } }
    return 0;
}
