# Runtime dependencies for the deployed Streamlit app.
# Streamlit Community Cloud installs exactly these versions during the build.
# Pin versions so the deployed app matches what you tested locally.
streamlit==1.38.0
pandas==2.2.2
numpy==1.26.4
matplotlib==3.9.2
plotly==5.24.0
