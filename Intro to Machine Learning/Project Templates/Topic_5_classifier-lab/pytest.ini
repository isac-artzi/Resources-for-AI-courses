# pytest configuration.
# pythonpath = . puts the project root on the import path so tests can do
# `from src...` and `from db...` no matter which folder pytest is launched from.
[pytest]
pythonpath = .
testpaths = tests
