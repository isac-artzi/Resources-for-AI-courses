# API tier — installed by Render.com (see render.yaml buildCommand).
# Keep this list minimal: every megabyte is cold-start time on the free plan.
#
# The CPU-only build of torch is roughly a quarter the size of the default one,
# which ships CUDA libraries this instance has no GPU to use. The index line
# below is what gets you the small one.
--extra-index-url https://download.pytorch.org/whl/cpu

fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
torch==2.4.1
transformers==4.44.2
tokenizers==0.19.1
supabase==2.7.4
python-dotenv==1.0.1

# NOT here on purpose: datasets, accelerate, peft, trl. Those belong in your
# OFFLINE training environment. The web service loads a checkpoint and generates
# from it; it never trains, so it should not carry the training stack.
